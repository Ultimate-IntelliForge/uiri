# Root-level env — Turborepo remote cache (optional)
# TURBO_TOKEN="your-vercel-remote-cache-token"
# TURBO_TEAM="your-vercel-team-slug"

# Per-app variables live in apps/api/.env and apps/web/.env
# Copy those .env.example files and fill in before running `npm run dev`
